<Window x:Class="ProblemSolver.UI.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProblemSolver.UI"
        mc:Ignorable="d"
        Title="ConfigWindow" Height="480" Width="800"
        MinHeight="480"
        MinWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <Label Content="Neural Network Model:"/>
            <ComboBox ItemsSource="{Binding NeuralNetworkModels}" SelectedItem="{Binding Config.NeuralNetworkModel}"/>
            <Label Content="Language:"/>
            <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding Config.Language}"/>
            <Label Content="Task ID:"/>
            <TextBox Text="{Binding Config.TaskId}"/>
            <Label Content="Tasks:"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="200">
                <ListView ItemsSource="{Binding Config.Tasks}" Margin="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                            <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="300"/>
                            <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="300"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </ScrollViewer>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
